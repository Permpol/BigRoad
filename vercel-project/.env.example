# Copy this to .env.local and set your values

# Password for login (change this!)
APP_PASSWORD=your-secret-password-here

# JWT secret key (use a long random string, e.g. openssl rand -hex 32)
JWT_SECRET=your-jwt-secret-key-change-this-to-random-string
